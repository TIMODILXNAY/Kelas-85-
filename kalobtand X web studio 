
<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kalobtand X • Premium Web Studio</title>
    <meta name="description" content="Studio web premium dengan desain futuristik dan performa kelas dunia.">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* ===== LUXURY VARIABLES ===== */
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;800&family=Outfit:wght@300;500;700&display=swap');
        
        :root {
            --primary: #6366f1;
            --primary-glow: rgba(99, 102, 241, 0.4);
            --bg-dark: #050508;
            --card-bg: rgba(255, 255, 255, 0.03);
            --border: rgba(255, 255, 255, 0.08);
            --text-main: #ffffff;
            --text-dim: #94a3b8;
            --transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; scroll-behavior: smooth; }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: var(--text-main);
            background: var(--bg-dark);
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* ===== BACKGROUND AMBIENCE ===== */
        .mouse-glow {
            position: fixed;
            width: 600px; height: 600px;
            background: radial-gradient(circle, var(--primary-glow) 0%, transparent 70%);
            border-radius: 50%;
            pointer-events: none;
            z-index: -1;
            transform: translate(-50%, -50%);
            transition: 0.1s ease-out;
        }

        .grid-lines {
            position: fixed;
            inset: 0;
            background-image: linear-gradient(var(--border) 1px, transparent 1px), linear-gradient(90deg, var(--border) 1px, transparent 1px);
            background-size: 50px 50px;
            mask-image: radial-gradient(circle at center, black, transparent 80%);
            z-index: -2;
            opacity: 0.5;
        }

        .container { width: 90%; max-width: 1200px; margin: 0 auto; }

        /* ===== NAVIGATION ===== */
        header {
            position: fixed;
            width: 100%; top: 0; z-index: 1000;
            padding: 30px 0;
            transition: var(--transition);
        }

        header.scrolled {
            padding: 15px 0;
            background: rgba(5, 5, 8, 0.8);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border);
        }

        .navbar { display: flex; justify-content: space-between; align-items: center; }

        .logo {
            font-size: 1.6rem; font-weight: 800; text-decoration: none;
            color: #fff; display: flex; align-items: center; gap: 10px;
            letter-spacing: -1px;
        }

        .logo span { color: var(--primary); }

        .nav-links { display: flex; gap: 35px; align-items: center; }
        .nav-links a { 
            text-decoration: none; color: var(--text-dim); 
            font-weight: 500; font-size: 0.95rem; transition: 0.3s;
        }
        .nav-links a:hover { color: var(--primary); }

        .menu-toggle { display: none; color: #fff; font-size: 1.5rem; cursor: pointer; }

        /* ===== HERO SECTION ===== */
        .hero {
            padding: 220px 0 120px;
            text-align: center;
            position: relative;
        }

        .badge {
            display: inline-flex; align-items: center; gap: 8px;
            padding: 8px 16px; background: var(--card-bg);
            border: 1px solid var(--border); color: var(--primary);
            border-radius: 100px; font-size: 0.75rem; font-weight: 700;
            margin-bottom: 30px; letter-spacing: 1px;
            text-transform: uppercase;
        }

        .hero-title {
            font-family: 'Outfit', sans-serif;
            font-size: clamp(2.8rem, 10vw, 5.5rem);
            font-weight: 700; line-height: 1; margin-bottom: 25px;
            background: linear-gradient(180deg, #fff 30%, rgba(255,255,255,0.5) 100%);
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
            letter-spacing: -3px;
        }

        .hero-subtitle {
            font-size: 1.25rem; color: var(--text-dim);
            max-width: 650px; margin: 0 auto 45px;
            font-weight: 300;
        }

        .btn {
            padding: 18px 40px; background: var(--primary);
            color: white; text-decoration: none; border-radius: 100px;
            font-weight: 700; transition: var(--transition);
            box-shadow: 0 20px 40px var(--primary-glow);
            display: inline-flex; align-items: center; gap: 12px;
        }

        .btn:hover {
            transform: translateY(-8px) scale(1.02);
            box-shadow: 0 25px 50px var(--primary-glow);
            filter: brightness(1.1);
        }

        /* ===== SERVICES BENTO GRID ===== */
        .section-padding { padding: 120px 0; }
        
        .section-header { text-align: center; margin-bottom: 70px; }
        .section-header h2 { font-size: 3rem; font-family: 'Outfit'; margin-bottom: 15px; }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 25px;
        }

        .card {
            background: var(--card-bg);
            padding: 50px 40px;
            border-radius: 32px;
            border: 1px solid var(--border);
            transition: var(--transition);
            position: relative; overflow: hidden;
        }

        .card:hover {
            border-color: var(--primary);
            background: rgba(255, 255, 255, 0.05);
            transform: translateY(-10px);
        }

        .card i {
            font-size: 2.5rem; color: var(--primary);
            margin-bottom: 30px; display: block;
        }

        .card h3 { font-size: 1.6rem; margin-bottom: 15px; }
        .card p { color: var(--text-dim); font-size: 1rem; }

        /* ===== ANIMATION ===== */
        .reveal { opacity: 0; transform: translateY(50px); transition: 1.2s cubic-bezier(0.16, 1, 0.3, 1); }
        .reveal.active { opacity: 1; transform: translateY(0); }

        /* ===== MOBILE RESPONSIVE ===== */
        @media (max-width: 768px) {
            .nav-links {
                position: fixed; top: 0; right: -100%; width: 80%; height: 100vh;
                background: #0a0a0f; flex-direction: column; justify-content: center;
                transition: 0.5s; z-index: 2000;
            }
            .nav-links.active { right: 0; }
            .menu-toggle { display: block; position: relative; z-index: 2100; }
            .hero-title { font-size: 3.5rem; letter-spacing: -1px; }
        }
    </style>
</head>
<body>

    <div class="mouse-glow" id="mouseGlow"></div>
    <div class="grid-lines"></div>

    <header id="header">
        <div class="container navbar">
            <a href="#" class="logo">
                <span>KX</span> STUDIO
            </a>
            <div class="menu-toggle" id="menuToggle">
                <i class="fas fa-bars-staggered"></i>
            </div>
            <nav class="nav-links" id="navLinks">
                <a href="#services">Layanan</a>
                <a href="#portfolio">Proyek</a>
                <a href="#about">Tentang</a>
                <a href="https://wa.me/6285167932764" class="btn" style="padding: 10px 25px; font-size: 0.85rem;">
                    Free Audit
                </a>
            </nav>
        </div>
    </header>

    <section class="hero">
        <div class="container">
            <div class="reveal active">
                <span class="badge"><i class="fas fa-sparkles"></i> Future of Web 2025</span>
                <h1 class="hero-title">Crafting Digital<br>Masterpieces</h1>
                <p class="hero-subtitle">Kami tidak hanya membuat website. Kami membangun identitas digital yang mendominasi pasar dan memikat audiens global.</p>
                <div class="hero-btns">
                    <a href="#services" class="btn">
                        Mulai Kolaborasi <i class="fas fa-chevron-right"></i>
                    </a>
                </div>
            </div>
        </div>
    </section>

    <section class="section-padding" id="services">
        <div class="container">
            <div class="section-header reveal">
                <span style="color: var(--primary); font-weight: 800;">SERVICES</span>
                <h2>Solusi Tanpa Batas</h2>
            </div>
            
            <div class="grid">
                <div class="card reveal">
                    <i class="fas fa-wand-magic-sparkles"></i>
                    <h3>Visual Identity</h3>
                    <p>Desain UI/UX yang artistik namun fungsional, dirancang untuk meningkatkan retensi pengguna hingga 200%.</p>
                </div>
                <div class="card reveal">
                    <i class="fas fa-microchip"></i>
                    <h3>Core Development</h3>
                    <p>Arsitektur kode yang bersih dan scalable menggunakan teknologi stack terbaru untuk performa kilat.</p>
                </div>
                <div class="card reveal">
                    <i class="fas fa-chart-pie"></i>
                    <h3>Digital Strategy</h3>
                    <p>Integrasi SEO dan analitik mendalam untuk memastikan investasi digital Anda menghasilkan ROI nyata.</p>
                </div>
            </div>
        </div>
    </section>

    <footer style="padding: 60px 0; border-top: 1px solid var(--border); text-align: center;">
        <p style="color: var(--text-dim); font-size: 0.9rem;">&copy; 2025 Kalobtand X Studio. Crafted for Excellence.</p>
    </footer>

    <script>
        // Mouse Glow Effect
        const glow = document.getElementById('mouseGlow');
        document.addEventListener('mousemove', (e) => {
            glow.style.left = e.clientX + 'px';
            glow.style.top = e.clientY + 'px';
        });

        // Sticky Header
        window.addEventListener('scroll', () => {
            const header = document.getElementById('header');
            header.classList.toggle('scrolled', window.scrollY > 50);

            // Reveal Animation
            const reveals = document.querySelectorAll('.reveal');
            reveals.forEach(reveal => {
                const revealTop = reveal.getBoundingClientRect().top;
                if (revealTop < window.innerHeight - 100) {
                    reveal.classList.add('active');
                }
            });
        });

        // Mobile Menu
        const menuToggle = document.getElementById('menuToggle');
        const navLinks = document.getElementById('navLinks');

        menuToggle.addEventListener('click', () => {
            navLinks.classList.toggle('active');
            menuToggle.querySelector('i').classList.toggle('fa-bars-staggered');
            menuToggle.querySelector('i').classList.toggle('fa-xmark');
        });
    </script>
</body>
</html>
